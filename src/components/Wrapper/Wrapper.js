import styled from "styled-components";

const Wrapper = styled.div`
  margin-left: auto;
  margin-right: auto;
  max-width: 1200px;
  padding-right: 10px;
  padding-left: 10px;
`;

export default Wrapper;
